PLAN=READY

MAPPING: PASS for a bounded transfer test, not source equivalence. Five fresh-process pairs isolate BM at M=2048: BM128/BM64=1.8183x, low CV, exact-equal output. Grid [40,16,1] -> [40,32,1] proves BM controls CTA rows while effective output coverage stays 128 columns/CTA; Bee `BM 128->64`, `BN=128`, `WN=2`, 256 threads is the right geometry to test. Official `BN=64` is inferred as each of two 64-column bands. Warp ownership, decoded-B lifetime, and fifth integer `2->3` are unproved/unmapped and must not be copied.

SCOPE: Permit one compile-guarded change in `escha-moe.cu`: launch existing `escha_matmul_dense_tiled_mma<3,64,128,false>` under the predicate, add a unique tag, compute BM64 smem/grid, and make A-stage layout BM-dependent. Derive `CPB=(BM*16*sizeof(half))/256` (8/16), prove exact coverage, and use an 8-byte `uint2` sync copy for BM64 plus CPB=8 async copies. Retaining sync `uint4` would overlap/cross rows. This is mechanical fallout preserving the 256-thread invariant, not a second variable.

Forbidden: global `ESCHA_MMA_BM` change; BN/WN/thread-count/accumulator changes; official fifth-parameter imitation; split-K retuning; packed code/decode, payload, shared-B, ldmatrix/HMMA, fragment store, partial layout, rotate/finalize, fusion, or non-target routing changes. Gate-off behavior must remain the promoted Stage-2 control.

PREDICATE and EXPECTED ROUTE/GRID: compile guard AND `use_mma && !gen && K==3 && IC==17408 && OC==5120 && n_rows==2048`; no filename/model/benchmark heuristic. The Escha op is metadata-selected; standard Qwen cannot enter it. Exact 2048 is the only evidenced M and avoids split-K retuning. Preserve `n_slices=1`; grid `[32,40,1]`, block `[32,8,1]`, dynamic smem 9,728 B. Expect 64 target calls/request; the established two-request/800-call proof must show 128 candidate, 672 unchanged, zero mismatches. Rows 17/2047/2049/4096 and non-target shapes retain control routes.

CORRECTNESS + RESOURCE GATES: Before timing: (1) Gate-off diff/route equals starting control `4bc1afc1d`. (2) Static/exhaustive BM64 A-map check shows 256 threads cover every `[64][16]` half exactly once, no overlap/OOB, for async and sync paths. (3) P-ARCH-20 lane-store check: candidate remains `FP16_ACC=false`; FP32 loop is unchanged, bounds `l<tile_c::ne`, and exhaustive `(warp,i,j,l)` enumeration covers every 64x128 output once and only once, with all `acc[i][j].x[l]` accesses in bounds. Also confirm the fixed FP16 `tile_ah` store is untouched (2 half2 registers, both x/y lanes; no OOB), even though this route does not execute it. (4) Identical-input packed-op BM64 vs BM128 at target: byte equality, max-abs=0, rel-RMS=0, finite output; predicate boundary/non-target outputs byte-identical to control. (5) P2/P7 greedy seed-42: 16/16 each; decode r5 regression <=2%; no CUDA errors/hangs. (6) Per-symbol resource proof: <=128 regs/thread, STACK=0, LOCAL=0, zero spills; static/dynamic shared allocation recorded and dynamic expected 9,728 B. Any failure rejects before performance.

BENCHMARK + ACCEPT/REJECT GATES: Freeze/hash control and candidate binaries/model/config. First require target-family steady-state matmul median gain >=15%; any other family regression >5% rejects. Then run the pre-registered 9 adjacent matched pairs, one unrecorded warmup/arm, graphs ON/profile OFF, order `AB BA BA AB AB BA BA AB AB`, exact shared 2048 IDs, `-p 2048 -n 0 -b 2048 -ub 2048`, identical clocks/telemetry, no competing GPU work. If both arm CVs <=2%, accept only full-2K median gain >=5%. Otherwise use the existing fallback: paired-log G>=1.05, 95% t-CI lower bound >1.00, and candidate faster >=8/9. Reject on full-2K gain <5%, fallback failure, route/resource/correctness failure, or any family >5% slower. Scheduler `3029 tok/s` is excluded from the canonical boundary: `3029*0.6238 ~= 1890`, not 2048. Server comparison is `2048/client_wall_seconds` (warm evidence 634.9 ms -> 3225.7 tok/s), with server e2e 619.678 ms reported separately; profiler-covered prefill duration and observed graph capture remain unknown/false respectively.

ROLLBACK: On any decisive failure, `git revert <EXP-06 implementation SHA>` (or restore only the target hunk to `4bc1afc1d`), retain evidence/docs, and require `git diff 4bc1afc1d -- ggml/src/ggml-cuda/escha-moe.cu` to be empty. Do not roll back to protected ARCH-01 `4501b3ee1`.

EVIDENCE GAPS: Official evidence does not prove Bee speed transfer, warp ownership, shared-B lifetime, or the fifth integer. Bee BM64 compile/SASS/resource, exactness, real-model route, quality, decode, and performance proof remain. WSL profiler coverage is unknown; scheduler reported CUDA graph false.

VERDICT=CONFIRM
